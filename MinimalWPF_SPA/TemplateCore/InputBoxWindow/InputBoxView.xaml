<Window
    x:Class="System.Windows.InputBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:System.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="420"
    Height="220"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="TxtMessage"
            Margin="0,0,0,10"
            TextWrapping="Wrap" />

        <ContentControl x:Name="InputHost" Grid.Row="1" />

        <TextBlock
            x:Name="TxtError"
            Grid.Row="2"
            Margin="0,10,0,0"
            Foreground="Red" />

        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Right"
            Orientation="Horizontal">

            <Button
                x:Name="BtnOk"
                Width="90"
                Height="30"
                Margin="0,0,5,0"
                Click="BtnOk_Click" />

            <Button
                x:Name="BtnCancel"
                Width="90"
                Height="30"
                Click="BtnCancel_Click" />

        </StackPanel>

    </Grid>
</Window>
