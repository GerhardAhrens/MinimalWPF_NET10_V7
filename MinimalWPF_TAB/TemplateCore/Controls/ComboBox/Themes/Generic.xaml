<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UI="clr-namespace:System.Windows.Controls">

    <!--  Brushes  -->
    <SolidColorBrush x:Key="WindowComboBoxBackgroundBrush" Color="#FFF9F9F9" />
    <SolidColorBrush x:Key="WindowComboBoxBackgroundHoverBrush" Color="#FFF3F3F3" />
    <SolidColorBrush x:Key="WindowComboBoxBackgroundPressedBrush" Color="#FFECECEC" />
    <SolidColorBrush x:Key="WindowComboBoxBackgroundDisabledBrush" Color="#FFF3F3F3" />
    <SolidColorBrush x:Key="WindowComboBoxForegroundBrush" Color="#FF1A1A1A" />
    <SolidColorBrush x:Key="WindowComboBoxForegroundDisabledBrush" Color="#FF9A9A9A" />
    <SolidColorBrush x:Key="WindowComboBoxBorderBrush" Color="#FF8A8A8A" />
    <SolidColorBrush x:Key="WindowComboBoxBorderHoverBrush" Color="#FF707070" />
    <SolidColorBrush x:Key="WindowComboBoxBorderPressedBrush" Color="#FF5F5F5F" />
    <SolidColorBrush x:Key="WindowComboBoxBorderFocusBrush" Color="#FF0067C0" />
    <SolidColorBrush x:Key="WindowComboBoxSelectionBrush" Color="#190067C0" />
    <SolidColorBrush x:Key="WindowComboBoxSelectionHoverBrush" Color="#240067C0" />
    <SolidColorBrush x:Key="WindowComboBoxRevealBrush" Color="#18000000" />
    <SolidColorBrush x:Key="WindowComboBoxPopupBackgroundBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="WindowComboBoxPopupBorderBrush" Color="#18000000" />
    <SolidColorBrush x:Key="WindowComboBoxArrowBrush" Color="#FF4A4A4A" />

    <!--  Animation  -->
    <Storyboard x:Key="WindowComboBoxOpenStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="DropDownBorder"
            Storyboard.TargetProperty="Opacity"
            From="0"
            To="1"
            Duration="0:0:0.08" />

        <DoubleAnimation
            Storyboard.TargetName="DropDownScaleTransform"
            Storyboard.TargetProperty="ScaleY"
            From="0.96"
            To="1"
            Duration="0:0:0.12">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>


    <!--  Validation Error Template  -->
    <ControlTemplate x:Key="WindowComboBoxValidationErrorTemplate">
        <Grid>
            <AdornedElementPlaceholder />
            <Border
                Margin="-2"
                BorderBrush="#FFD13438"
                BorderThickness="1.5"
                CornerRadius="7"
                IsHitTestVisible="False" />
        </Grid>
    </ControlTemplate>

    <!--  ComboBoxItem  -->
    <Style x:Key="WindowComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Foreground" Value="{StaticResource WindowComboBoxForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Margin" Value="4,2" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border
                        x:Name="ItemBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="4">

                        <Grid>
                            <!--  Selection indicator  -->
                            <Border
                                x:Name="SelectionIndicator"
                                Width="3"
                                Margin="-7,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Stretch"
                                Background="#FF0067C0"
                                CornerRadius="2"
                                Opacity="0" />

                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Pointer over  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="#0D000000" />
                        </Trigger>

                        <!--  Selected  -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource WindowComboBoxSelectionBrush}" />
                            <Setter TargetName="SelectionIndicator" Property="Opacity" Value="1" />
                        </Trigger>

                        <!--  Selected + Hover  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>

                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource WindowComboBoxSelectionHoverBrush}" />

                        </MultiTrigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource WindowComboBoxForegroundDisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--  WindowComboBox  -->
    <Style TargetType="{x:Type UI:WindowComboBox}">
        <Setter Property="Foreground" Value="{StaticResource WindowComboBoxForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource WindowComboBoxBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource WindowComboBoxBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource WindowComboBoxValidationErrorTemplate}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type UI:WindowComboBox}">
                    <Grid>

                        <!--  Main Control  -->
                        <Border
                            x:Name="OuterBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding UI:WindowComboBox.CornerRadius}">

                            <Grid>
                                <!--  Reveal-like highlight  -->
                                <Border
                                    x:Name="RevealBorder"
                                    Margin="1"
                                    Background="{StaticResource WindowComboBoxRevealBrush}"
                                    CornerRadius="{TemplateBinding UI:WindowComboBox.CornerRadius}"
                                    IsHitTestVisible="False"
                                    Opacity="0" />

                                <!--  Non editable content  -->
                                <ContentPresenter
                                    x:Name="ContentSite"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    IsHitTestVisible="False"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                                <!--  Editable TextBox  -->
                                <TextBox
                                    x:Name="PART_EditableTextBox"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontSize="{TemplateBinding FontSize}"
                                    Foreground="{TemplateBinding Foreground}"
                                    IsReadOnly="{TemplateBinding IsReadOnly}"
                                    Visibility="Collapsed" />

                                <!--  DropDown Button  -->
                                <ToggleButton
                                    x:Name="DropDownToggle"
                                    Width="36"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Stretch"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Focusable="False"
                                    IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">

                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border
                                                x:Name="ToggleBorder"
                                                Background="Transparent"
                                                CornerRadius="0,6,6,0">

                                                <Path
                                                    x:Name="Arrow"
                                                    Width="10"
                                                    Height="6"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 5 5 L 10 0"
                                                    Stretch="None"
                                                    Stroke="{StaticResource WindowComboBoxArrowBrush}"
                                                    StrokeThickness="1.5" />
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ToggleBorder" Property="Background" Value="#0D000000" />
                                                </Trigger>

                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ToggleBorder" Property="Background" Value="#18000000" />
                                                </Trigger>

                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>
                        </Border>

                        <!--  Popup  -->
                        <Popup
                            x:Name="PART_Popup"
                            AllowsTransparency="True"
                            Focusable="False"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                            PopupAnimation="None"
                            StaysOpen="False">

                            <Border
                                x:Name="DropDownBorder"
                                MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                Background="{StaticResource WindowComboBoxPopupBackgroundBrush}"
                                BorderBrush="{StaticResource WindowComboBoxPopupBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Opacity="0">

                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="DropDownScaleTransform" CenterX="0.5" CenterY="0" ScaleX="1" ScaleY="0.96" />
                                </Border.RenderTransform>

                                <Border.Effect>
                                    <DropShadowEffect
                                        BlurRadius="18"
                                        Direction="270"
                                        Opacity="0.18"
                                        ShadowDepth="4" />

                                </Border.Effect>

                                <ScrollViewer
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    CanContentScroll="True"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Auto">

                                    <ItemsPresenter />

                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>

                    <!--  Triggers  -->
                    <ControlTemplate.Triggers>
                        <!--  Mouse Hover  -->

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource WindowComboBoxBackgroundHoverBrush}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource WindowComboBoxBorderHoverBrush}" />
                            <Setter TargetName="RevealBorder" Property="Opacity" Value="0.5" />
                        </Trigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsPressed, ElementName=DropDownToggle}" Value="True" />
                                <Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource TemplatedParent}}" Value="True" />
                            </MultiDataTrigger.Conditions>

                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource WindowComboBoxBackgroundPressedBrush}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource WindowComboBoxBorderPressedBrush}" />

                        </MultiDataTrigger>

                        <!--  Keyboard Focus  -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource WindowComboBoxBorderFocusBrush}" />
                            <Setter TargetName="OuterBorder" Property="BorderThickness" Value="1.5" />
                        </Trigger>


                        <!--  DropDown Open  -->
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource WindowComboBoxBorderFocusBrush}" />
                            <Setter TargetName="OuterBorder" Property="BorderThickness" Value="1.5" />

                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>

                                        <DoubleAnimation
                                            Storyboard.TargetName="DropDownBorder"
                                            Storyboard.TargetProperty="Opacity"
                                            From="0"
                                            To="1"
                                            Duration="0:0:0.10" />

                                        <DoubleAnimation
                                            Storyboard.TargetName="DropDownScaleTransform"
                                            Storyboard.TargetProperty="ScaleY"
                                            From="0.96"
                                            To="1"
                                            Duration="0:0:0.12">

                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>

                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>


                        <!--  Editable  -->
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource WindowComboBoxBackgroundDisabledBrush}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#33000000" />
                            <Setter Property="Foreground" Value="{StaticResource WindowComboBoxForegroundDisabledBrush}" />
                            <Setter TargetName="RevealBorder" Property="Opacity" Value="0" />
                        </Trigger>

                        <!--  Validation Error  -->
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#FFD13438" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
