<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UI="clr-namespace:System.Windows.Controls">

    <!--
        ============================================================
        Colors
        ============================================================
    -->

    <SolidColorBrush x:Key="WindowCheckBoxForeground" Color="#1A1A1A" />
    <SolidColorBrush x:Key="WindowCheckBoxBorder" Color="#707070" />
    <SolidColorBrush x:Key="WindowCheckBoxBorderHover" Color="#0067C0" />
    <SolidColorBrush x:Key="WindowCheckBoxChecked" Color="#0067C0" />
    <SolidColorBrush x:Key="WindowCheckBoxCheckedHover" Color="#1976D2" />
    <SolidColorBrush x:Key="WindowCheckBoxDisabled" Color="#9A9A9A" />
    <SolidColorBrush x:Key="WindowCheckBoxCheckMark" Color="White" />

    <!--
        ============================================================
        Focus Adorner
        ============================================================
    -->

    <Style x:Key="WindowCheckBoxFocusVisual" TargetType="{x:Type Control}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        Margin="-4"
                        BorderBrush="#0067C0"
                        BorderThickness="2"
                        CornerRadius="6"
                        SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <!--
        ============================================================
        WindowCheckBox
        ============================================================
    -->
    <Style TargetType="{x:Type UI:WindowCheckBox}">
        <Setter Property="Foreground" Value="{StaticResource WindowCheckBoxForeground}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FocusVisualStyle" Value="{StaticResource WindowCheckBoxFocusVisual}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type UI:WindowCheckBox}">
                    <Grid
                        MinHeight="32"
                        Background="Transparent"
                        SnapsToDevicePixels="True">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!--  CheckBox  -->
                        <Border
                            x:Name="CheckBoxBorder"
                            Width="18"
                            Height="18"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderBrush="{StaticResource WindowCheckBoxBorder}"
                            BorderThickness="1.2"
                            CornerRadius="4"
                            RenderTransformOrigin="0.5,0.5">

                            <Border.RenderTransform>

                                <ScaleTransform x:Name="CheckBoxScale" ScaleX="1" ScaleY="1" />

                            </Border.RenderTransform>


                            <!--  Check / Indeterminate  -->
                            <Grid>

                                <!--  Check mark  -->
                                <Path
                                    x:Name="CheckMark"
                                    Width="11"
                                    Height="11"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="M 1,5.5 L 4.5,9 L 10,1"
                                    Stroke="{StaticResource WindowCheckBoxCheckMark}"
                                    StrokeEndLineCap="Round"
                                    StrokeStartLineCap="Round"
                                    StrokeThickness="2"
                                    Visibility="Collapsed" />

                                <!--  Indeterminate  -->
                                <Border
                                    x:Name="IndeterminateMark"
                                    Width="9"
                                    Height="2"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="{StaticResource WindowCheckBoxCheckMark}"
                                    CornerRadius="1"
                                    Visibility="Collapsed" />

                            </Grid>
                        </Border>

                        <!--  Content  -->
                        <ContentPresenter
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentStringFormat="{TemplateBinding ContentStringFormat}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            TextElement.FontFamily="{TemplateBinding FontFamily}"
                            TextElement.FontSize="{TemplateBinding FontSize}"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Grid>

                    <!--  Animations  -->
                    <ControlTemplate.Resources>
                        <!--  Hover  -->
                        <Storyboard x:Key="MouseOverStoryboard">

                            <ColorAnimation
                                Storyboard.TargetName="CheckBoxBorder"
                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                To="#0067C0"
                                Duration="0:0:0.12" />
                        </Storyboard>

                        <!--  Leave  -->
                        <Storyboard x:Key="MouseLeaveStoryboard">

                            <ColorAnimation
                                Storyboard.TargetName="CheckBoxBorder"
                                Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                To="#707070"
                                Duration="0:0:0.12" />

                        </Storyboard>

                        <!--  Press  -->
                        <Storyboard x:Key="PressedStoryboard">
                            <DoubleAnimation
                                Storyboard.TargetName="CheckBoxScale"
                                Storyboard.TargetProperty="ScaleX"
                                To="0.92"
                                Duration="0:0:0.08" />

                            <DoubleAnimation
                                Storyboard.TargetName="CheckBoxScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="0.92"
                                Duration="0:0:0.08" />

                        </Storyboard>


                        <!--  Release  -->
                        <Storyboard x:Key="ReleasedStoryboard">
                            <DoubleAnimation
                                Storyboard.TargetName="CheckBoxScale"
                                Storyboard.TargetProperty="ScaleX"
                                To="1"
                                Duration="0:0:0.12" />

                            <DoubleAnimation
                                Storyboard.TargetName="CheckBoxScale"
                                Storyboard.TargetProperty="ScaleY"
                                To="1"
                                Duration="0:0:0.12" />

                        </Storyboard>
                    </ControlTemplate.Resources>

                    <!--  Triggers  -->
                    <ControlTemplate.Triggers>

                        <!--  Checked  -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource WindowCheckBoxChecked}" />
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource WindowCheckBoxChecked}" />
                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Indeterminate  -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource WindowCheckBoxChecked}" />
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource WindowCheckBoxChecked}" />
                            <Setter TargetName="IndeterminateMark" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Mouse Over  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource MouseOverStoryboard}" />
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource MouseLeaveStoryboard}" />
                            </Trigger.ExitActions>
                        </Trigger>

                        <!--  Checked + Mouse Over  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>

                            <Setter TargetName="CheckBoxBorder" Property="Background" Value="{StaticResource WindowCheckBoxCheckedHover}" />
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource WindowCheckBoxCheckedHover}" />

                        </MultiTrigger>


                        <!--  Pressed  -->
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedStoryboard}" />
                            </Trigger.EnterActions>

                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource ReleasedStoryboard}" />
                            </Trigger.ExitActions>

                        </Trigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>

                        <!--  Keyboard focus  -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#0067C0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
